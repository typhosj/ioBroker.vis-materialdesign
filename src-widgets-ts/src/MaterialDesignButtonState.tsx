import { createButtonClass } from './MaterialDesignButtons';

export default createButtonClass({ id: 'tplVis-materialdesign-Button-State', name: 'Button State', kind: 'state', layout: 'default', label: '&nbsp;State', icon: 'pencil' });
